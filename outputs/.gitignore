# Git does not track empty directories by default.
# However, we want the outputs/ directory to always be accesible.

#Ignore everything in this directory
*

# Except for the .gitignore file itself
!.gitignore

# Other files not to ignore
!expected_variants_*.tsv
!observed_variants_stats*.tsv
!constrained_transcripts*.tsv
!panel_app_gms_rare_disease_genes.tsv

